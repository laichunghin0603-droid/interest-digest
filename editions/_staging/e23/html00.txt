<!DOCTYPE html>
<html lang="zh-Hant">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="theme-color" content="#f5f5f7">
<title>晚間興趣速報 · 2026-09-23</title>
<style>
:root {
  --canvas: #ffffff;
  --section: #f5f5f7;
  --card: #ffffff;
  --text: #1d1d1f;
  --secondary: #6e6e73;
  --tertiary: #86868b;
  --blue: #0071e3;
  --blue-hover: #0077ed;
  --red: #ff3b30;
  --hairline: rgba(0,0,0,0.08);
  --hairline-solid: #d2d2d7;
  --material: rgba(255,255,255,0.66);
  --material-edge: rgba(255,255,255,0.55);
  --font: -apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display",
    "Helvetica Neue", "PingFang HK", "Noto Sans TC", sans-serif;
  --ease-out: cubic-bezier(0.25, 0.1, 0.25, 1);
  --press-duration: 100ms;
  --crossfade: 200ms;
}
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { -webkit-text-size-adjust: 100%; height: 100%; }
body {
  font-family: var(--font);
  background: var(--section);
  color: var(--text);
  font-size: 17px;
  font-weight: 400;
  line-height: 1.47;
  -webkit-font-smoothing: antialiased;
  min-height: 100%;
  min-height: 100dvh;
}
a { color: var(--blue); text-decoration: none; }
a:hover { text-decoration: underline; }
.page {
  max-width: 980px;
  margin: 0 auto;
  padding: 0 22px 80px;
  padding-left: max(22px, env(safe-area-inset-left, 0px));
  padding-right: max(22px, env(safe-area-inset-right, 0px));
  padding-bottom: calc(80px + env(safe-area-inset-bottom, 0px));
}
.tabbar-wrap {
  position: sticky;
  top: 0;
  z-index: 40;
  background: var(--material);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  backdrop-filter: blur(20px) saturate(180%);
  border-bottom: 1px solid var(--hairline);
  box-shadow: inset 0 1px 0 var(--material-edge);
  margin: 0 calc(-1 * max(22px, env(safe-area-inset-left, 0px))) 0 calc(-1 * max(22px, env(safe-area-inset-right, 0px)));
  padding: 12px max(22px, env(safe-area-inset-left, 0px)) 14px max(22px, env(safe-area-inset-right, 0px));
  padding-top: calc(12px + env(safe-area-inset-top, 0px));
}
.tabs {
  display: flex; flex-wrap: nowrap; gap: 8px;
  overflow-x: auto; -webkit-overflow-scrolling: touch;
  scrollbar-width: none; padding: 2px 0;
}
.tabs::-webkit-scrollbar { display: none; }
.tab {
  flex: 0 0 auto; appearance: none;
  border: 1px solid var(--ha